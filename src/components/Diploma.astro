---
interface Props {
    image:string;
    current: string;
}

const {  image } = Astro.props;
const { current } = Astro.props;
---

<img style={current} src={image} class="w-full rounded-md " alt="dploma"/>
